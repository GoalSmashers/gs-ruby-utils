#!/usr/bin/env ruby

case ARGV.length
when 0
  system('rake spec')
when 1
  if ARGV[0] =~ /_spec.rb$/
    system("ruby -Ilib -Ispec #{ARGV[0]}")
  else
    system("IN=#{ARGV[0]} rake spec:group")
  end
end
